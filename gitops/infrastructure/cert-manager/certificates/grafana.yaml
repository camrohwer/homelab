apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: grafana
  namespace: monitoring

spec:
  secretName: grafana-tls

  dnsNames:
    - grafana.homelab.local

  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
