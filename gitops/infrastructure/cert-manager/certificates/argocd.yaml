apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: argocd
  namespace: argocd

spec:
  secretName: argocd-tls

  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer

  dnsNames:
    - argocd.homelab.local
