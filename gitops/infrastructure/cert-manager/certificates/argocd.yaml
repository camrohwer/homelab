apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: argocd
  namespace: argocd

spec:
  secretName: argocd-tls

  dnsNames:
    - argocd.homelab.local

  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
