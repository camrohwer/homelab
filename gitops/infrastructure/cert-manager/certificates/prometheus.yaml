apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: prometheus
  namespace: monitoring

spec:
  secretName: prometheus-tls

  dnsNames:
    - prometheus.homelab.local

  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
